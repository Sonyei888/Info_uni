<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icono_info_grande"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icono_info_grande_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Uniinfo"

        tools:targetApi="31">
        <activity
            android:name=".AdminEditarNoticia"
            android:exported="false" />
        <activity
            android:name=".AdminVerNoticias"
            android:exported="false" />
        <activity
            android:name=".usuAcercade"
            android:exported="false" />
        <activity
            android:name=".AdminAgregarNoticia"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".usuNoticia"
            android:exported="false" />

        <activity
            android:name=".UserFuncionalidades"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>